apiVersion: v1
kind: Service
metadata:
  name: webui-rollout
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: webui-rollout


---

apiVersion: v1
kind: Service
metadata:
  name: webui-rollout-proxy
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: webui-rollout-proxy
